{
  "name": "axsdk",
  "module": "index.ts",
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "bump": "node scripts/bump-and-publish.mjs",
    "bump:patch": "node scripts/bump-and-publish.mjs patch",
    "bump:minor": "node scripts/bump-and-publish.mjs minor",
    "bump:major": "node scripts/bump-and-publish.mjs major",
    "bump:dry": "node scripts/bump-and-publish.mjs patch --dry-run",
    "build": "node scripts/set-workspace-deps.mjs && cd packages/axsdk-voice && bun install && bun run build && cd ../axsdk-core && bun install && bun run build && cd ../axsdk-react && bun install && bun run build && cd ../axsdk-browser && bun install && bun run build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "@types/bun": "latest",
      "@types/node": "latest"
    }
  }
}
